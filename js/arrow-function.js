"use strict";
// arrow function typescript
const classRoom = (chair, table) => chair + table;
// multi line arrow function
const doMath = (x, y) => {
    return x * y;
};
// if dosen't need to return
const fullName = (firstName, lastName, suriname) => {
};
