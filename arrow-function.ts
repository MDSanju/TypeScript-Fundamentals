// arrow function typescript
const classRoom = (chair: number, table: number): number => chair + table;

// multi line arrow function
const doMath = (x: number, y: number): number => {
    return x * y;
}

// if dosen't need to return
const fullName = (firstName: string, lastName: string, suriname: string): void => {

}